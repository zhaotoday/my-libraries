## 命令
```bash
# JS 校验，在根目录下执行
$ node_modules/.bin/eslint {lib}/src/**/*.js

# CSS 校验，在根目录下执行
$ node_modules/.bin/stylelint {lib}/src/**/*.css

# 开发
$ cd {lib}
$ npm run dev

# 构建
$ cd {lib}
$ npm run build
```
